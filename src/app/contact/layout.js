
export const generateMetadata = ()=>{
    return {
        title:'Contact generateMetadata',
    }
};

const layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default layout;